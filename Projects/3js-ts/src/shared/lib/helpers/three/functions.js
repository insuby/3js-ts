/**
 *
 * @param material
 * @param hexadecimal
 */
// export const highlightMaterial = (material, hexadecimal = 0x110000) => {
//   if (!material) return;
//   material.emissive.setHex(null);
// };

/**
 * @param {object} mesh
 * @param {number} color
 */
export const changeMaterialColor = (mesh, color) => {
  // if (!!!mesh.userData) return;
  // const { colorOrigin, colorHighlight } = mesh.userData;
  // mesh.material.color.set(color === colorOrigin ? color : colorOrigin);
};
